#!/usr/bin/python3
def max_integer(my_list=[]):
    length(len(my_list))
    if (length == 0):
        return "None"
    else:
        max = my_list[0]
        for c in range(length):
            if my_list[c] > max:
                max = my_list[c]
        return max
